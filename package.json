{
  "name": "vim-candle",
  "version": "0.0.1",
  "description": "This plugin created to learn golang.",
  "scripts": {
    "watch": "chokidar '**/*.go' -c 'npm run build'",
    "build": "cd ./go && go build -ldflags '-s -w' && mkdir -p ../bin/candle && mv ./go ../bin/candle/candle-server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hrsh7th/vim-candle.git"
  },
  "author": "hrsh7th",
  "license": "ISC",
  "homepage": "https://github.com/hrsh7th/vim-candle",
  "devDependencies": {
    "chokidar-cli": "^2.1.0",
    "husky": "^4.0.6"
  }
}
